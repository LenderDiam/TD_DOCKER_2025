# =============================================================================
# API Configuration - Environment Variables Template
# =============================================================================
# ⚠️  NOTE: This file is ONLY a reference documentation
# 
# For use with Docker:
#   → Use the /.env file at the ROOT of the project
#   → Variables are automatically passed via docker-compose.yml
#
# For local development (without Docker):
#   → Copy this file to ./api/.env
#   → Change DB_HOST=localhost instead of DB_HOST=db
# =============================================================================

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
# HTTP port on which the API server listens
PORT=3000

# Node environment (development, production, test)
NODE_ENV=development

# -----------------------------------------------------------------------------
# Database Connection (PostgreSQL)
# -----------------------------------------------------------------------------
# Database host (use 'db' for Docker, 'localhost' for local development)
DB_HOST=db

# Database port (default PostgreSQL port: 5432)
DB_PORT=5432

# Database user
DB_USER=td_user

# Database password
DB_PASSWORD=td_password

# Database name
DB_NAME=td_db

# Database connection pool settings (optional)
# Maximum number of clients in the pool
DB_POOL_MAX=10

# Idle timeout in milliseconds
DB_POOL_IDLE_TIMEOUT=30000

# Connection timeout in milliseconds
DB_POOL_CONNECTION_TIMEOUT=2000

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Allowed origins for CORS (use '*' for all, or specific URLs like 'http://localhost:8080')
CORS_ORIGIN=*

# Allow credentials in CORS requests (true/false)
CORS_CREDENTIALS=true

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Log level (error, warn, info, debug)
LOG_LEVEL=info

# Pretty print logs in development (true/false)
LOG_PRETTY=true
