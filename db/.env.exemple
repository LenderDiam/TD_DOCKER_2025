# =============================================================================
# Database Service - Environment Variables Template
# =============================================================================
#   NOTE: This file is for reference only
# 
# For use with Docker:
#   → All variables are configured in the root /.env file
#   → Passed to the container via docker-compose.yml
#
# These variables are used by the PostgreSQL container
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Configuration
# -----------------------------------------------------------------------------

# Database username
# Used for authentication and initial user creation
POSTGRES_USER=td_user

# Database password
# CRITICAL: Change this in production!
# Never commit actual passwords to version control
POSTGRES_PASSWORD=td_password

# Database name
# The name of the database to create on first startup
POSTGRES_DB=td_db

# PostgreSQL port (internal)
# Default: 5432 (standard PostgreSQL port)
DB_PORT=5432

# =============================================================================
# Notes
# =============================================================================
# 1. These variables must match the API's DB_* variables
# 2. POSTGRES_* variables are PostgreSQL-specific
# 3. The init scripts use these credentials automatically
# 4. Changes require container recreation: docker compose up -d --force-recreate db
# =============================================================================
