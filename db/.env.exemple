# =============================================================================
# Database Service - Environment Variables Template
# =============================================================================
# ⚠️  NOTE: This file is for reference only
# 
# For Docker:
#   → Use the root /.env file
#   → Variables are passed via docker-compose.yml
# =============================================================================

# PostgreSQL user
POSTGRES_USER=td_user

# PostgreSQL password (⚠️ Change in production!)
POSTGRES_PASSWORD=td_password

# Database name
POSTGRES_DB=td_db

# PostgreSQL port
DB_PORT=5432

# =============================================================================
# Note: These variables must match the API's DB_* configuration
# =============================================================================
